# Function definition
def greet(name)
    "Hello, #{name}"
end

# Class definition
class Person
    def initialize(name, age)
        @name = name
        @age = age
    end

    def greet
        "Hello, my name is #{@name}"
    end
end
